version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    # Disable routine version-update PRs; Dependabot security updates remain enabled.
    open-pull-requests-limit: 0
    ignore:
      # Adopt new Node type majors only with a deliberate runtime-support review.
      - dependency-name: '@types/node'
        update-types: ['version-update:semver-major']
    groups:
      development:
        dependency-type: development
        update-types: [minor, patch]
      production:
        dependency-type: production
        update-types: [minor, patch]
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Disable routine version-update PRs.
    open-pull-requests-limit: 0
    groups:
      actions:
        patterns: ['*']
